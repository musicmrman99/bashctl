Syntax:
    <{name:funcsafe}> --help[=<action>]

Description:
    -h, -?, --help
        Display help. Without an argument, display the main help message. With
        an argument, display the individual help message of the given action, or
        the main help message if '<{name:funcsafe}>' is given.
